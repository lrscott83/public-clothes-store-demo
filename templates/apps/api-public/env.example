# Bare Phase-0 scaffold: `/health` returns a literal `{ status: 'ok' }` and
# never touches Postgres, so no DATABASE_URL is needed yet. It arrives with
# the real tenant guard/DB-backed health check in Phase 4.
PORT=3003
