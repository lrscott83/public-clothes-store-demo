/**
 * `/tiendas` — platform console store list (design D6). Behavior lands with
 * its own TDD cycle (`platform/routes/__tests__/tiendas.test.tsx`); this
 * stub exists so the `_platform` branch registers cleanly.
 */
export default function TiendasPage() {
  return <p>Cargando tiendas…</p>;
}
