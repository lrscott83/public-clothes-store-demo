/**
 * `/tiendas/nueva` — platform console create-on-behalf form (design D6).
 * Behavior lands with its own TDD cycle (extends
 * `platform/routes/__tests__/tiendas.test.tsx`); this stub exists so the
 * `_platform` branch registers cleanly.
 */
export default function TiendasNuevaPage() {
  return <p>Cargando formulario…</p>;
}
